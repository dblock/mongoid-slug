language: ruby

sudo: false

rvm:
  - 2.2.2
  - 2.3.1
  - jruby-9.0.5.0

gemfile:
  - Gemfile

env:
  global:
    - JRUBY_OPTS="--server -J-Xms512m -J-Xmx1024m"

services:
- mongodb

addons:
  apt:
    sources:
    - mongodb-3.2-precise
    packages:
    - mongodb-org-server

notifications:
  email: false
